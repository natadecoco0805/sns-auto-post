name: Discord Post Test

on:
  workflow_dispatch:
  schedule:
    - cron: "7 23,1,3,6,9,12,14 * * *"

jobs:
  post:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests google-genai
        
      - name: Run Script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          DISCORD_WEBHOOK_INVESTMENT: ${{ secrets.DISCORD_WEBHOOK_INVESTMENT }}
          DISCORD_WEBHOOK_BEAUTY: ${{ secrets.DISCORD_WEBHOOK_BEAUTY }}
          DISCORD_WEBHOOK_GADGET: ${{ secrets.DISCORD_WEBHOOK_GADGET }}
        run: python discord_post.py
